import Foundation

extension MenuEventReducer {
    enum DidSelectCampReducer {
        static func handleEvent(
            camp: Camp,
            state: inout GrouperState
        ) -> [GrouperAction] {
            []
        }
    }
}
